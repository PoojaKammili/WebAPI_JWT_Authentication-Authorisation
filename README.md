# WebAPI_JWT_Authentication&Authorisation

📘 Description
This is a C# ASP.NET Core Web API project demonstrating Authorization and Authentication using JWT (JSON Web Tokens).  
The API secures endpoints and ensures only authenticated users with proper roles can access specific resources.

🚀 Features
- User registration and login with JWT token generation
- Role-based Authorization (e.g., Admin, User)
- Secures Web API endpoints using `[Authorize]` attribute
- Clean project structure with Models, Controllers, Services, and Middleware
- Demonstrates token validation, expiration, and refresh token mechanism

🧱 Tech Stack
- Language: C#
- Framework: .NET 6 / .NET 7
- Authentication: JWT (JSON Web Token)
- Database: SQL Server / In-memory database for demo
- IDE: Visual Studio / VS Code
